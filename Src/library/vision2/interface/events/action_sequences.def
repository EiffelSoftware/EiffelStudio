#===============================================================================
# Action sequence definitions for EiffelVision2
# $Date$
# $Revision$
# NOTE: This is not supported: x, y: INTEGER
#       Do this instead: x: INTEGER; y: INTEGER
#===============================================================================

ev_notify: for general notify events with no parameters;

ev_pointer_button: for mouse button up/down events; x: INTEGER; y: INTEGER; button: INTEGER; x_tilt: DOUBLE; y_tilt: DOUBLE; pressure: DOUBLE; screen_x: INTEGER; screen_y: INTEGER

ev_pointer_button_left_press = ev_pointer_button (-,-,=1,-,-,>=0.5,-,-): for left mouse button down events

ev_pointer_motion: for mouse movement events; x: INTEGER; y: INTEGER; x_tilt: DOUBLE; y_tilt: DOUBLE; pressure: DOUBLE; screen_x: INTEGER; screen_y: INTEGER

ev_proximity: for proximity in/out events

ev_accelerator: for accelerator events

ev_key: for keyboard events; keycode: INTEGER

ev_key_string: for keyboard events; keystring: STRING

ev_geometry: for geometry related events; x: INTEGER; y: INTEGER; width: INTEGER; height: INTEGER

ev_pnd_start: for pick and drop transport start events; x: INTEGER; y: INTEGER

ev_item_select: for list item selection events; index: INTEGER; item: EV_LIST_ITEM

# FIXME
# Here we could pass the previous focused widget: previous: EV_WIDGET
# Or for focus lose the newly focused widget...
ev_focus: for lose/gain of keyboard-focus
