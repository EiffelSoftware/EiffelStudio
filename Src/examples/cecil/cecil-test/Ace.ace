system cecil

root

	main (root_cluster): "make"

default
	assertion (require);
	console_application (yes);

cluster

	root_cluster:	"$ISE_LIBRARY/examples/cecil/cecil-test"
		visible
			MAIN;
		end

	all base :	"$ISE_LIBRARY/library/base"
		exclude
			"desc";"table_eiffel3";
		visible
			STRING;
			LINKED_LIST;
		end

end

