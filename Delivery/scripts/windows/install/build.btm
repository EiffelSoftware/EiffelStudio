iff not defined INIT_DIR then
	set INIT_DIR=%@EXECSTR[cd]\..
endiff

iff .%ISE_PLATFORM%.==.win64. then
	%INIT_DIR%\wix\candle -sw1044 -dIsWin64=yes %1.wxs
else
	%INIT_DIR%\wix\candle -sw1044 -dIsWin64=no %1.wxs
endiff
%INIT_DIR%\wix\light /b %INSTALL_DIR %1.wixobj -out MSMs\%1.msm
del %1.wixobj
