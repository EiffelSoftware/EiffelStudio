A sequence of changes where a rescue clause is added to external
routines, then removed and routines are made deferred causes the
compiler to die with a segmentation fault in `has_rescue'.

Discovered in Release 5.0.015.
