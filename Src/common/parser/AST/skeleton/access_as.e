indexing

	description: "Abstract description of a non-nested call";
	date: "$Date$";
	revision: "$Revision$"

deferred class ACCESS_AS

inherit

	CALL_AS

feature

	access_name: STRING is
		deferred
		end;

end -- class ACCESS_AS
