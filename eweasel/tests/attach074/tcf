
test_name 	vevi-on-once-per-object-routine
test_description A void-safe system with a class that includes a once-per-object routine causes the compiler to incorrectly report a VEVI error on the hidden EXCEPTION attribute generated by the compiler.

copy_sub Ace $TEST Ace
copy_raw test.e $CLUSTER test.e

compile_melted
compile_result ok

test_end
