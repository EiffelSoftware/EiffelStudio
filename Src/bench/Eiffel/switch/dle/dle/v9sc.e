-- The feature was statically bound in the DR-system but needs
-- to be dynamically bound after loading the DC-set.

class V9SC 

inherit

	FEATURE_ERROR
	
feature 

	code: STRING is "V9SC";
			-- Error code

end
