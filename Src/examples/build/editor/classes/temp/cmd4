class COMMAND4

inherit

	NON_UNDOABLE_CMD

creation

	make

feature

	back_label: STRING is "back";

	argument1: SCROLLED_T;

	execute is
		do
			set_transition_label (back_label);
		end;

	make (arg1: SCROLLED_T) is
		do
			argument1 := arg1;
		end;

end
