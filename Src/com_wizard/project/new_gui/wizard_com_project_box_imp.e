indexing
	description: "Objects that represent an EV_TITLED_WINDOW.%
		%The original version of this class was generated by EiffelBuild."

deferred class
	WIZARD_COM_PROJECT_BOX_IMP

inherit
	EV_VERTICAL_BOX
		redefine
			initialize, is_in_default_state
		end
			
	WIZARD_CONSTANTS
		undefine
			is_equal, default_create, copy
		end

-- This class is the implementation of an EV_TITLED_WINDOW generated by EiffelBuild.
-- You should not modify this code by hand, as it will be re-generated every time
-- modifications are made to the project.

feature {NONE}-- Initialization

	initialize is
			-- Initialize `Current'.
		do
			Precursor {EV_VERTICAL_BOX}
			initialize_constants
			
				-- Create all widgets.
			create com_project_label
			create definition_file_box
			
				-- Build_widget_structure.
			extend (com_project_label)
			extend (definition_file_box)
			
			set_minimum_width (390)
			set_padding_width (7)
			disable_item_expand (com_project_label)
			disable_item_expand (definition_file_box)
			com_project_label.set_text ("A definition file is either a Interface Definition file (*.idl) or a type library file (*.tlb).%NA type library can be linked into a component (*.ocx, *.dll, *.exe, ...) ")
			com_project_label.align_text_left
			
				--Connect events.
				-- Close the application when an interface close
				-- request is recieved on `Current'. i.e. the cross is clicked.

				-- Call `user_initialization'.
			user_initialization
		end

feature -- Access

	com_project_label: EV_LABEL
	definition_file_box: WIZARD_FILE_PATH_BOX

feature {NONE} -- Implementation

	is_in_default_state: BOOLEAN is
			-- Is `Current' in its default state?
		do
			-- Re-implement if you wish to enable checking
			-- for `Current'.
			Result := True
		end
	
	user_initialization is
			-- Feature for custom initialization, called at end of `initialize'.
		deferred
		end
	
end -- class WIZARD_COM_PROJECT_BOX_IMP
