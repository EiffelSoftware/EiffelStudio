<?xml version="1.0"?>
<project name="build_net" default="help">

	<description>
		description: "EiffelNET (ipv6) library compilation"
	</description>

	<target name="help">
		<echo message="usage:"/>
		<echo message=" geant compile"/>
		<echo message=" geant clean"/>
		<echo message=" geant clobber"/>
	</target>
	
	<target name="compile">
		<echo message="- Compile [EiffelNet]" />
		<geant target="compile" file="build.eant" dir="Clib" 
			reuse_variables="true" />
	</target>
	
	<target name="clean">
		<echo message="- Clean [EiffelNet]" />
		<delete directory="spec/${ISE_PLATFORM}" unless="${is_windows}"/>
		<delete directory="spec\${ISE_C_COMPILER}\${ISE_PLATFORM}" if="${is_windows}" />
	</target>

	<target name="clobber" depend="clean" />
	
</project>
