class B

feature -- Procedures

	p0 is
		do
		end

	p1 (x: BOOLEAN) is
		do
		end

	p2 (x, y: BOOLEAN) is
		do
		end

	p3 (x, y, z: BOOLEAN) is
		do
		end

feature -- Functions with different number of arguments

	f0: BOOLEAN is
		do
		end

	f1 (x: BOOLEAN): BOOLEAN is
		do
		end

	f2 (x, y: BOOLEAN): BOOLEAN is
		do
		end

	f3 (x, y, z: BOOLEAN): BOOLEAN is
		do
		end

end