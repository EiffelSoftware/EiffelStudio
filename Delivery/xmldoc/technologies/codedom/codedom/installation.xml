<document title="Installation">
	<meta_data/>
	<paragraph>
		For certain tools (including ASP.NET) to detect the availability of a CodeDom Provider, the corresponding
		 assembly name must be listed in the .NET machine wide configuration file typically located in: 
		
		<code_block>%SYSTEM_DRIVE%\Windows\Microsoft.NET\Framework\vx.x.xxxx\CONFIG\machine.config</code_block>

		 (where vx.x.xxxx corresponds to the .NET Framework version number). 
		The available CodeDom Providers are listed under the <italic>&lt;compilers&gt;</italic> XML node. The 
		Eiffel CodeDom Provider implements a.NET Installer class so that should you need to add
		 the Eiffel CodeDom Provider to the machine configuration file, simply run the .NET SDK command line 
		utility <italic>InstallUtil</italic> as follows:

		<code_block>InstallUtil EiffelSoftware.Codedom.dll</code_block>

		Conversely, should the Eiffel Codedom Provider not be required anymore, running the following
		 command line will remove it from the <italic>machine.config</italic> file:

		<code_block>InstallUtil /u EiffelSoftware.Codedom.dll</code_block>

		<note>
			<bold>Note</bold>: The <bold>EiffelASP.NET</bold> installation program will take care of registering 
			and unregistering the Eiffel CodeDom Provider properly.
		</note>
		<seealso>
			<bold>See also</bold>: 
			<link>
				<url>supported_constructs.xml</url>
				<label>Supported constructs</label>
			</link>
		</seealso>
	</paragraph>
</document>
