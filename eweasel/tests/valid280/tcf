test_name	bogus-vjar-error
test_description If you have an assignment of æn expression where one component involves an anchored type to an argument, the compiler resolves the type of the expression incorrectly.

copy_sub Ace $TEST Ace
copy_raw test.e $CLUSTER test.e

compile_melted
compile_result	ok

test_end
