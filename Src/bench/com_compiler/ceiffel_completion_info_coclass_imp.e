indexing
	description: "CEIFFEL_COMPLETION_INFO_COCLASS Implementation."
	Note: "Automatically generated by the EiffelCOM Wizard."

class
	CEIFFEL_COMPLETION_INFO_COCLASS_IMP

inherit
	CEIFFEL_COMPLETION_INFO_COCLASS

creation
	make,
	make_from_pointer

feature {NONE}  -- Initialization

	make is
			-- Creation. Implement if needed.
		do
			
		end

	make_from_pointer (cpp_obj: POINTER) is
			-- Creation.
		do
			set_item (cpp_obj)
			make
		end

feature -- Basic Operations

	create_item is
			-- Initialize `item'
		do
			item := ccom_create_item (Current)
		end

	target_features (target: STRING; feature_name: STRING; file_name: STRING): IENUM_COMPLETION_ENTRY_INTERFACE is
			-- Features accessible from target.
			-- `target' [in].  
			-- `feature_name' [in].  
			-- `file_name' [in].  
		do
			-- Put Implementation here.
		end

feature {NONE}  -- Externals

	ccom_create_item (eif_object: like Current): POINTER is
			-- Initialize `item'
		external
			"C++ [new ecom_eiffel_compiler::CEiffelCompletionInfo %"ecom_eiffel_compiler_CEiffelCompletionInfo_s.h%"](EIF_OBJECT)"
		end

end -- CEIFFEL_COMPLETION_INFO_COCLASS_IMP

