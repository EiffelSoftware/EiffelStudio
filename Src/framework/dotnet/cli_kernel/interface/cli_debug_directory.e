note
	description: "Summary description for {CLI_DEBUG_DIRECTORY}."
	date: "$Date$"
	revision: "$Revision$"

deferred class
	CLI_DEBUG_DIRECTORY

feature -- Element change	

	set_time_date_stamp (a_time_date_stamp: INTEGER)
		deferred
		end

end
