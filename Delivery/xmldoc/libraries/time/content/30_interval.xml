<document output="studio" title="Interval">
	<meta_data/>
	<paragraph>
		<paragraph>Class <code><class_name>INTERVAL</class_name></code> deals with intervals between two instances of the same class which conform to<code><class_name> ABSOLUTE</class_name></code> (
			<code>
				<class_name>DATE</class_name>
			</code>, 
			<code>
				<class_name>TIME</class_name>
			</code>, 
			<code>
				<class_name>DATE_TIME</class_name>
			</code>). The notions of interval is directly linked with the notion of order.
		</paragraph>
		<paragraph>The <code><feature_name>start_bound</feature_name></code> must be before the <code><feature_name>end_bound</feature_name></code>.</paragraph>
		<heading>
			<size>4</size>Creation
		</heading>
		<paragraph>The features <code><feature_name>make</feature_name></code>, <code><feature_name>set_start_bound</feature_name></code> and <code><feature_name>set_end_bound</feature_name></code> take clones of their arguments, so that if these arguments are changed, the interval previously created is not.</paragraph>
		<paragraph>It would have been possible to create intervals with references to date or time, but a modification of the dates would have been effective in the interval so that only the invariant would have been able to check if the <code><feature_name>start_bound</feature_name></code> is still before the <code><feature_name>end_bound</feature_name></code>.</paragraph>
		<heading>
			<size>4</size>Interval measurement
		</heading>
		<paragraph>The measure of intervals is made by duration: the result is an instance of the class 
			<code>
				<class_name>DURATION</class_name>
			</code>. However, as 
			<code>
				<class_name>DURATION</class_name>
			</code> is the common parent of 
			<code>
				<class_name>TIME_DURATION</class_name>
			</code>, 
			<code>
				<class_name>DATE_DURATION</class_name>
			</code> and 
			<code>
				<class_name>DATE_TIME_DURATION</class_name>
			</code>, it does not have many features available. Some features in class 
			<code>
				<class_name>TIME</class_name>
			</code>, 
			<code>
				<class_name>DATE</class_name>
			</code>, 
			<code>
				<class_name>DATE_TIME</class_name>
			</code> return the same result and are more efficient to use. 
			<code>
				<class_name>DURATION</class_name>
			</code> has to be use as the last solution.
		</paragraph>
		<heading>
			<size>4</size>Comparison
		</heading>
		<paragraph>It includes intersection, inclusion and a special comparison.</paragraph>
		<list ordered="false">
			<item><code><feature_name>is_equal</feature_name></code> is present and compare values, not references.</item>
			<item>Feature <code><feature_name>intersects</feature_name></code> returns the mathematical result of the intersection of two intervals.</item>
			<item><code><feature_name>is_strict_included_by</feature_name></code>, <code><feature_name>strict_includes</feature_name></code>, <code><feature_name>is_included_by</feature_name></code> and <code><feature_name>includes</feature_name></code> are connected to the same notion of inclusion.</item>
			<item>&lt;, + &gt; and &gt;= use a special rule to compare intervals. int1 &lt; int2 is true if int1 starts and ends strictly before int2. The other features use the <code><feature_name>same rule</feature_name></code> and<code><feature_name> is_equal</feature_name></code> if needed.</item>
			<item><code><feature_name>overlaps</feature_name></code> looks like <code><feature_name>intersects</feature_name></code> but the argument has to be after the current interval. <code><feature_name>is_overlapped</feature_name></code> is the opposite.</item>
			<item><code><feature_name>meets</feature_name></code> and <code><feature_name>is_met</feature_name></code> are used to test if two intervals have a common bound.</item>
		</list>
		<heading>
			<size>4</size>Status Report
		</heading>
		<paragraph>The main part of the functions need an argument from the same generic type and return a <code><class_name>BOOLEAN</class_name></code> value.</paragraph>
		<list ordered="false">
			<item><code><feature_name>empty</feature_name></code> tests if the bounds are equal.</item>
			<item><code><feature_name>has</feature_name></code>,<code><feature_name> strict_before</feature_name></code>, <code><feature_name>strict_after</feature_name></code>,<code><feature_name> before</feature_name></code> and <code><feature_name>after</feature_name></code> test the position of an element relatively to the current interval.</item>
		</list>
		<heading>
			<size>4</size>Element change
		</heading>
		<paragraph><code><feature_name>set_start_bound</feature_name></code> and <code><feature_name>set_end_bound</feature_name></code> are available to change the bounds.</paragraph>
		<heading>
			<size>4</size>Operations
		</heading>
		<paragraph><code><feature_name>Union</feature_name></code> and <code><feature_name>intersection</feature_name></code> are the mathematical functions. <code><feature_name>gather</feature_name></code> requires that two intervals meet each other and then yields the union.</paragraph>
	</paragraph></document>
