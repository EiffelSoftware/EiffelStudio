VZAA, chapter 24 (Interfaces with other  languages),  page
  405

     The expression $` f' is valid if and only  if `f'  is
     the final name of  a feature  of the enclosing class,
     and that  feature  is not a constant attribute (which
     has no address).
