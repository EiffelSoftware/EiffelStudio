system
	race

root
	RACE(root_cluster): "make"

default

	assertion(require);
	multithreaded (yes);
	-- If you have the MT precompiled EiffelBase library, you may 
	-- the following default assertion: (check the name
	-- of your MT precompiled library in the specified path first.
	precompiled ("$ISE_EIFFEL/precomp/spec/$ISE_PLATFORM/base-mt");
	exception_trace (no)

cluster

	root_cluster: 		"$ISE_EIFFEL/examples/thread/race";
				-- If you intend to modify any part of the example,
				-- copy all the files of $ISE_EIFFEL/examples/thread/race
				-- into one of your directories, and replace
				-- $ISE_EIFFEL/examples/thread/race by the name of
				-- that directory on the above line.

-- EiffelBase

	all base:           "$ISE_EIFFEL/library/base"
		exclude
			"desc";"table_eiffel3"
		end

-- EiffelThread

	thread:				"$ISE_EIFFEL/library/thread";

end

