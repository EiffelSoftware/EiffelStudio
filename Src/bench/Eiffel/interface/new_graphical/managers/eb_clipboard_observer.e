indexing
	description	: "Observer for notification of clipboard changes."
	date		: "$Date$"
	revision	: "$Revision $"

deferred class
	EB_CLIPBOARD_OBSERVER

feature -- Status report

	on_changed is
			-- Clipboard has just changed?
		do
		end

end -- class EB_CLIPBOARD_OBSERVER
