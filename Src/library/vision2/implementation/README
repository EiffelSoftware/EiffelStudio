Directory structure of vision.
interface
implementation
	implementation_interface	
	gtk
		support = supportive classes specific to gtk implemantation
		widgets = widget classes
			primitives = primitive widget classes
			containers = container widget classes
		kernel = classes which are not widgets (font, pixmap, etc) but are common for every implemantation
	mswin
