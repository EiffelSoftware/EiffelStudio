<document title="Configuration">
	<meta_data/>
	<paragraph>
		The Eiffel Codedom Provider uses XML configuration files that define additional settings specific to Eiffel. 
		Such settings include whether to fail on error, what events to log, where to log them etc... Each application can 
		define its own configuration to be used when it loads the Eiffel CodeDom Provider. If an application is not associated 
		with a configuration file then a default configuration is used.
		
		<paragraph>
			The association between applications and configurations as well as the actual settings the configuration
			 defines can be set in the 
			<link><label>Eiffel CodeDom Provider Manager</label><url>../tools/manager.xml</url></link>. 
			Run <italic>ecdpman.exe</italic> to start the manager.
		</paragraph>

		<note>
			<bold>Note:</bold> The EiffelASP.NET installation program will add a shortcut in the start menu to the
			 Eiffel CodeDom Provider Manager.
		</note>
		
		<seealso>
			<bold>See also:</bold> <link><url>../tools/manager.xml</url><label>Eiffel CodeDom Provider Manager</label></link>, 
			<link><url>logging.xml</url><label>Logging</label></link>, 
			 <link><url>required_permissions.xml</url><label>Required permissions</label></link>
		</seealso>
	</paragraph>
</document>
