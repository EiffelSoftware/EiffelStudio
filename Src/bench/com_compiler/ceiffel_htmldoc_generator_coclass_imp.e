indexing
	description: "CEIFFEL_HTMLDOC_GENERATOR_COCLASS Implementation."
	Note: "Automatically generated by the EiffelCOM Wizard."

class
	CEIFFEL_HTMLDOC_GENERATOR_COCLASS_IMP

inherit
	CEIFFEL_HTMLDOC_GENERATOR_COCLASS

creation
	make,
	make_from_pointer

feature {NONE}  -- Initialization

	make is
			-- Creation. Implement if needed.
		do
			
		end

	make_from_pointer (cpp_obj: POINTER) is
			-- Creation.
		do
			set_item (cpp_obj)
			make
		end

feature -- Access

	is_incompatible: BOOLEAN is
			-- is the project incompatible?
		do
			-- Put Implementation here.
		end

	is_corrupted: BOOLEAN is
			-- is the project corrupted?
		do
			-- Put Implementation here.
		end

	last_error: STRING is
			-- the last error
		do
			-- Put Implementation here.
		end

feature -- Basic Operations

	create_item is
			-- Initialize `item'
		do
			item := ccom_create_item (Current)
		end

	add_excluded_cluster (cluster_to_exclude: STRING) is
			-- Add excluded cluster
			-- `cluster_to_exclude' [in].  
		do
			-- Put Implementation here.
		end

	remove_excluded_cluster (excluded_cluster: STRING) is
			-- Remove excluded cluster
			-- `excluded_cluster' [in].  
		do
			-- Put Implementation here.
		end

	load_project (project_dir: STRING): BOOLEAN is
			-- Load a compiled project
			-- `project_dir' [in].  
		do
			-- Put Implementation here.
		end

	generate (generation_dir: STRING) is
			-- Generate the documentation.
			-- `generation_dir' [in].  
		do
			-- Put Implementation here.
		end

feature {NONE}  -- Externals

	ccom_create_item (eif_object: like Current): POINTER is
			-- Initialize `item'
		external
			"C++ [new ecom_eiffel_compiler::CEiffelHTMLDocGenerator %"ecom_eiffel_compiler_CEiffelHTMLDocGenerator_s.h%"](EIF_OBJECT)"
		end

end -- CEIFFEL_HTMLDOC_GENERATOR_COCLASS_IMP

