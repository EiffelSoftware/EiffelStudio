case $CONFIG in
'')
	if test ! -f config.sh; then
		(echo "Can't find config.sh."; exit 1)
	fi 2>/dev/null
	. ./config.sh
	;;
esac
case "$0" in
*/*) cd `expr X$0 : 'X\(.*\)/'` ;;
esac
echo "Extracting "."/Makefile (with variable substitutions)"
$spitshell >Makefile <<!GROK!THIS!
########################################################################
# Makefile generated from Makefile.SH on $DATE

SHELL = /bin/sh
AR = ar rc
CC = $cc
CTAGS = ctags
CFLAGS = $optimize $ccflags $large -I\$(EIFFEL5)/bench/spec/\$(PLATFORM)/include -I\$(EIFFEL_SRC)/C/run-time/include
MTCFLAGS = $optimize $mtccflags $large -I\$(EIFFEL5)/bench/spec/\$(PLATFORM)/include -I\$(EIFFEL_SRC)/C/run-time/include
SHAREDLINK = $sharedlink
LDSHAREDFLAGS = $ldsharedflags
LN = $ln
MAKE = make
MKDEP = $mkdep \$(DPFLAGS) --
RANLIB = $ranlib
RM = $rm -f
########################################################################
# Automatically generated parameters -- do not edit

SOURCES =  \$(LSRCS)

TIME_LIBS = libdatetime.a libmtdatetime.a libdatetime.so libdatetime.so
########################################################################
# New suffixes and associated building rules -- edit with care


all: \$(TIME_LIBS)
!GROK!THIS!
$spitshell >>Makefile <<'!NO!SUBS!'

SMODE = local.c network.c network_r.c storable.c

LSRCS = $(SMODE)

MT_OBJECTS = \
	MTdatetime.o

OBJECTS = \
	datetime.o

ALL_OBJECTS = $(OBJECTS) $(MT_OBJECTS)

MTdatetime.o: datetime.c
	$(CC) -c $(MTCFLAGS) $? -o $@

libmtdatetime.a: $(MT_OBJECTS)
	$(RM) $@
	$(AR) $@ $(MT_OBJECTS)
	$(RANLIB) $@

libmtdatetime.so: $(MT_OBJECTS)
	$(RM) $@
	$(SHAREDLINK) $(LDSHAREDFLAGS) $@ $(MT_OBJECTS)

libdatetime.a: $(OBJECTS)
	$(RM) $@
	$(AR) $@ $(OBJECTS)
	$(RANLIB) $@

libdatetime.so: $(OBJECTS)
	$(RM) $@
	$(SHAREDLINK) $(LDSHAREDFLAGS) $@ $(OBJECTS)

local_realclean::
	$(RM) $(TIME_LIBS) $(ALL_OBJECTS)
########################################################################
# Common rules for all Makefiles -- do not edit

emptyrule::

clean: local_clean
realclean: local_realclean
clobber: local_clobber

local_clean::
	$(RM) core *~ *.o

local_realclean:: local_clean

local_clobber:: local_realclean
	$(RM) Makefile config.sh

Makefile: Makefile.SH
	/bin/sh Makefile.SH

tags::
	$(CTAGS) -w *.[ch]
	$(CTAGS) -xw *.[ch] > tags

local_clobber::
	$(RM) tags

########################################################################
# Dependencies generated by make depend
# DO NOT DELETE THIS LINE -- make depend relies on it

# Put nothing here or make depend will gobble it up
.FORCE_DEPEND::
	@echo "You must run 'make depend' in $(TOP) first."; exit 1
!NO!SUBS!
chmod 644 Makefile
$eunicefix Makefile

