language: eiffel
before_script:
    - export current_dir=$PWD ; echo current_dir=$current_dir ; cd ..
    - curl -sSL https://www.eiffel.org/setup/install.sh | bash -s nightly > eiffel.rc
    - source ./eiffel.rc
    - echo `ec -version`
    - cd $current_dir
    - export EIFFEL_SRC=$PWD/Src
    - export EWEASEL=$PWD/eweasel
    - export ISE_LIBRARY=$EIFFEL_SRC
    - export XEBRA_LIBRARY=$EIFFEL_SRC/framework/web/xebra/eiffel_projects/library
    - echo Check projects compilation status...
    
branches:
    only:
        - master

git:
    depth: 1
  
script: 
    - compile_all -ecb -melt -list_failures -log_verbose -clean -options dotnet=false -l Src/Eiffel
    - compile_all -ecb -melt -list_failures -log_verbose -clean -options dotnet=false -l Src/framework
    - compile_all -ecb -melt -list_failures -log_verbose -clean -options dotnet=false -l Src/tools
group: stable
os: linux
