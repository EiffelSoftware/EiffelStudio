<document title="Dynamic library: Generated files" output="studio">
	<meta_data/>
	<paragraph>
		<paragraph>Basically, once the Eiffel definition file is created, the compiler will generate a set of files and will compile them to generate the Dynamic library into the 
			<italic>EIFGENs/target_name/W_code</italic> or 
			<italic>EIFGENs/target_name/F_code</italic> directory.
		</paragraph>
		<note>
			<bold>Note</bold>: To generate and compile these files, you have to indicate the definition file that should be used in the 
			<link>
				<url>../../25_project_settings/050_advanced_general.xml</url>
				<label>advanced node of the project settings</label>
			</link> of your system. This way EiffelStudio will know which one to use. If you do not specify any definition file, nothing will be generated.
		</note>
		<paragraph>The set of files is composed of:</paragraph>
		<div>
			<list ordered="false">
				<item>
					EIFGENs/target_name/W_code/<bold>system.def</bold> (or F_code)
				</item>
				<item>
					EIFGENs/target_name/W_code/<bold>edynlib.c</bold> (or F_code)
				</item>
				<item>
					$(ISE_EIFFEL)/studio/spec/$(ISE_PLATFORM)/templates/<bold>egc_dynlib.template</bold>
					<line_break/>this file will be copied during the compilation into the EIFGENs/target_name/W_code/E1 directory as 
					<bold>egc_dynlib.c</bold>
				</item>
				<item>
					$(ISE_EIFFEL)/studio/spec/$(ISE_PLATFORM)/include/<bold>egc_dynlib.h</bold>
				</item>
				<item>and the Makefile</item>
			</list>
		</div>
		<paragraph>The 
			<italic>system.def</italic> is only used on windows, it is the definition file used to generate a DLL when linking.<line_break/>
Information about the DLL
<code_block>LIBRARY demo.dll
DESCRIPTION DEMO.DLL</code_block>

The following are EXPORTed functions.
<code_block>EXPORTS</code_block>

This part concerns the run-time. It initializes the run-time and reclaim Eiffel objects.
<code_block>
;System
	init_rt
	reclaim_rt
</code_block>

The exported for the BAR class:
<code_block>; CLASS [BAR]
	get_string
	print_bar
</code_block>

The exported for the ROOT_CLASS class:
<code_block>; CLASS [ROOT_CLASS]
	make
	foo
	test_bar</code_block>
		</paragraph>
		<paragraph>The 
			<italic>edynlib.c</italic> file is the interface between the Eiffel system and the C code. It contains the declaration and the implementation of the function used to directly call the eiffel feature with its real name.
		</paragraph>
		<paragraph>The 
			<span>
				<italic>egc_dynlib.c</italic>
			</span> and 
			<span>
				<italic>egc_dynlib.h</italic>
			</span> files are used for operations performed by the run-time.
		</paragraph>
	</paragraph></document>
