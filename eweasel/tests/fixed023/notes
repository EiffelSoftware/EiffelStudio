An actual generic parameter which is invalid (because it is itself a
generic class but no actual generic parameters are supplied) causes
the compiler to die with an exception trace.

Fixed in Prerelease 93.02.17.

