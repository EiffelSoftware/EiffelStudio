/*
 * Jmakefile for ewb (client)
 */

;# Files used to build the daemon
SRC = proto.c eif_in.c eif_out.c init.c

;# Derived object file names
OBJ = \
|expand f!$(SRC) $(LSRC)!
	!f:\.[cl]=.o \
-expand \\

;# Where shared archive is located (path and name)
LIBDIR = ../shared

/* The following are understood by jmake to alter built-in commands */
RUN_TIME = ../../run-time
DPFLAGS = -I$(TOP) -I$(LIBDIR) -I$(RUN_TIME)
CFLAGS = $(DPFLAGS)

DependTarget()
NormalLibraryTarget(ewb,$(SRC),$(OBJ))

