<document title="Global preferences" output="studio">
	<meta_data/>
	<paragraph>This categories gathers non graphical preferences that are not related to a tool in particular. These preferences are: 
		<line_break/>
		<table><border>1</border>
			<row>
				<cell>Label in the preferences window</cell>
				<cell>Complete Description</cell>
			</row>
			<row>
				<cell>Text files format.</cell>
				<cell>This boolean preference sets whether .e files will be saved in DOSor UNIX format. If it is set to "True", files will be saved in DOS format,i.e. line separator will be made of a line feed and a carriage return.Otherwise, UNIX format will used, i.e. there will not be carriage returns.</cell>
			</row>
			<row>
				<cell>Command used to launch an external editor.</cell>
				<cell>You may open a class in an external editor by dropping the correspondingpebble on the external editor icon. This preference sets which commandline will be used to launch the external editor. The parameter "$target"will be replaced in the command with the file name corresponding to theclass, "$line" with the line number if this information is relevant, with 1  otherwise.</cell>
			</row>
			<row>
				<cell>Use external command to print?</cell>
				<cell>This sets whether you want to specify which command line will be usedto print texts.</cell>
			</row>
			<row>
				<cell>Command invoked to print a text.</cell>
				<cell>This is the command used to print a text if the previous preference,"Use external command to print?", is true. The parameter will replace  "$printer" with the printer name on GTK platforms, and "$target"  with the name of the file given to the external program (see nextpreference).</cell>
			</row>
			<row>
				<cell>Send a postscript file to this command?</cell>
				<cell>If it is set to False, the command line above will be called on a basictext file containing what you want to print. Otherwise, the file will bea postscript file with a formatting close to the one on the screen.</cell>
			</row>
			<row>
				<cell>Number of projects kept in the recent projects list</cell>
				<cell>This integral preference defines the maximum number of entries that should  be displayed in the 
					<bold>File/Recent projects</bold> menu.
				</cell>
			</row>
			<row>
				<cell>Internet browser (GTK platforms only)</cell>
				<cell>This sets the command line that should be used to open an internet browser  on a given HTML page. If present, "$url" will be replaced with the  URL of the HTML page. This is used to display help files.</cell>
			</row>
		</table>
		<seealso>
			<bold>See Also</bold>
			<line_break/>
			<link>
				<url>index.xml</url>
				<label>Other Preferences</label>
			</link>
		</seealso>
	</paragraph></document>
