indexing
	description: "Objects that represent hot zone when dragging on a tab stub sequence."
	date: "$Date$"
	revision: "$Revision$"

class
	SD_HOT_ZONE_SEQUENCE

inherit
	SD_HOT_ZONE

feature -- Implementation

	apply_change is
			-- Apply change when user dragging a window on a position
		do
			
		end
		
	update_for_pointer_position is
			-- Update feedback when user move the mouse.
		do
			
		end

invariant
	invariant_clause: True -- Your invariant here

end
