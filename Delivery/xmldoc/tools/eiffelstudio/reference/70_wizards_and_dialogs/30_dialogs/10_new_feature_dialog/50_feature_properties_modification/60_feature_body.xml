<document title="Feature body" output="studio">
	<meta_data/>
	<paragraph>
		<paragraph>What goes into the feature body field is dependent on what kind of feature you wish to generate.</paragraph>
		<list ordered="false">
			<item>
				<bold>do</bold>: this is the normal procedure type. Enter in the field below
				<bold>do</bold> a body for your feature.
				<code_block>
	do_something is
		do
			a := b
		end
</code_block>
			</item>
			<item>
				<bold>once</bold>: this is the type for a routine that is executed once per execution. As a once routine cannot have arguments, it is disabled when you added one or more arguments.
				<code_block>
	init is
		once
			load
		end
</code_block>
			</item>
			<item>
				<bold>deferred</bold>: this creates a routine of deferred type. The local and body fields grey out as they serve no use anymore.
				<code_block>
	do_something is
		deferred
		end
</code_block>
			</item>
			<item>
				<bold>external</bold>: this creates an Eiffel wrap routine around a routine written in another language. Use the body field to enter a string containing the necessary information about the routine using the external syntax.
				<code_block>
	do_something is
		external
			"C | %"location.h%""
		end
</code_block>
			</item>
		</list>
	</paragraph></document>