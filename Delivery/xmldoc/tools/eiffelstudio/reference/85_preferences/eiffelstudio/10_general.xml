<document title="General Preferences" output="studio">
	<meta_data/>
	<paragraph>This category gathers non graphical and general preferences that are not related to a tool in particular. These preferences are: 
		<line_break/>
		<table><border>1</border>
			<row>
				<cell>Label in the preferences window</cell>
				<cell>Complete Description</cell>
			</row>
			<row>
				<cell>Command used to launch an external editor.</cell>
				<cell>You may open a class in an external editor by dropping the corresponding pebble on the external editor icon. This preference sets which command line will be used to launch the external editor. The parameter "$target"will be replaced in the command with the file name corresponding to the class, "$line" with the line number if this information is relevant, with 1  otherwise.</cell>
			</row>		
			<row>
				<cell>Internet browser (GTK platforms only)</cell>
				<cell>This sets the command line that should be used to open an internet browser  on a given HTML page. If present, "$url" will be replaced with the  URL of the HTML page. This is used to display help files.</cell>
			</row>
		</table>
	</paragraph></document>
