An invalid type (ARRAY, which is invalid because ARRAY is a generic
class but no generic parameter is supplied) is accepted by the
compiler, but only when it is used as explicit creation type in a
creation instruction.

Discovered in Prerelease 93.05.17.

