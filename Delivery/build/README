EiffelBuild 4.2
===============

Installation
------------

- Set environment variable EIFFEL4

for example, setenv EIFFEL4 /usr/local/Eiffel4


- Set environment variable PLATFORM according to your platform.

for example, setenv PLATFORM linux-elf


- Untar or copy the files into EIFFEL4 directory.

cd $EIFFEL4
gunzip -c eiffelbuild-4.2.tar.gz | tar xvf -


- Add directory $EIFFEL4/build/spec/$PLATFORM into your path


- Run EiffelBuild

ebuild


