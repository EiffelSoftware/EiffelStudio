system
	test

root
	FIGURE_TEST: make_and_launch

default

	assertion (all)
	--debug("EVENT_TRACE")
	--debug("EV_GTK_DEBUG")
cluster

dev:						"$WS"

event(dev):					"$/event"

gel(dev):					"$/gel"

all vision2(dev):				"$/vision2"
	exclude
		"mswin";
		"EIFGEN";
	end

all base(dev):					"$/base"
	exclude
		"table_eiffel3"
	end

external

	include_path:
		"$WS/vision2/implementation/gtk/Clib", "`gtk-config --cflags`";
	object:
		"$(WS)/vision2/implementation/gtk/Clib/gtk_eiffel.o",
		"`gtk-config --libs`"

end
