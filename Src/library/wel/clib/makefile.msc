# wel.lib - Makefile for Microsoft C

CC = cl

# Finalized Version
CFLAGS = -nologo -c -Ox -W1 -I..\spec\windows\include -I$(ISE_EIFFEL)\bench\spec\windows\include -DNONAMELESSUNION

# Debug Version
#CFLAGS = -c -Zi -Od -W4 -I..\spec\windows\include -I$(ISE_EIFFEL)\bench\spec\windows\include -DNONAMELESSUNION

.c.obj:
	$(CC) $(CFLAGS) $<

OBJ = 	registry.obj \
		disptchr.obj \
		enumfont.obj \
		estream.obj \
		msgboxpa.obj \
		diskspace.obj \
		dynload.obj \
		choose_folder.obj \
		mousehook.obj

wel.lib: $(OBJ)
	if exist $@ del $@
	lib /NOLOGO /OUT:$@ $(OBJ)
	if not exist ..\spec\msc mkdir ..\spec\msc
	if not exist ..\spec\msc\lib mkdir ..\spec\msc\lib
	copy $@ ..\spec\msc\lib
	del $@
	del *.obj
